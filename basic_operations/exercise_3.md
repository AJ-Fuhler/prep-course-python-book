print('5' + '10') would output '510'. 
It would do this since '5' and '10' are strings.
So, instead of performing any arithmetic operation, 
Python will concatenate the strings instead, resulting in '510'.